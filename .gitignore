# Python bytecode + packaging metadata
__pycache__/
*.pyc
*.pyo
*.pyd
*.egg-info/

# Virtual environment
.venv/
venv/
env/

# IDE project files
.idea/
.vscode/

# User-provided game data (must be supplied locally; not committed)
0001-l044.xml
save_001.xml

# Generated scanner output
x4_empire_state.json

# Generated SQLite database
*.db
*.db-wal
*.db-shm

# Memory junction (points to ~/.claude/projects/.../memory; files live outside repo)
memory/

# OS junk
.DS_Store
Thumbs.db
*.pyc

# Throwaway test/temp scripts, caches, and fixtures
scratch/
.claude/

# Graphify knowledge graph artifacts (regenerable — the graph itself stays local)
graphify-out/
.graphifyignore

# Design-sync converter artifacts (regenerated each run; state anchored by uploaded _ds_sync.json)
.ds-sync/
ds-bundle/
.design-sync/.cache/
.design-sync/node_modules

# Design system (React lib — lives outside the main app's git history)
design-system/

docs/ALERT_PROMPTS.md
